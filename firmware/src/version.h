#pragma once

// Hand-bumped short version tag — short on purpose (screen is small; a
// timestamp was too many characters even at the smallest font). Bump this
// whenever you flash something worth being able to tell apart on-device:
// v1.0, v1.0a, v1.0b, ... v1.1, ... Shown as a tiny corner label on the
// splash screen only (ui.cpp) so a fresh flash is visually confirmable
// without a serial monitor.
#define FIRMWARE_VERSION "v2.0"
